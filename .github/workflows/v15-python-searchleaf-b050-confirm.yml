name: V15 Python search-leaf H64 b050 192-game confirmation

on:
  push:
    branches:
      - analysis/v15-python-searchleaf-b050-confirm-20260909
    paths:
      - .github/workflows/v15-python-searchleaf-b050-confirm.yml
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  confirm:
    runs-on: ubuntu-24.04
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check chess==1.11.2 numpy==2.5.2 numba==0.67.0
      - name: Download exact packaged V14
        uses: actions/download-artifact@v4
        with:
          name: Little-Gambit-V14-PACKAGE-ae1dc230b9208f5ec46c9a661c3f0e526d6f0e56
          path: v14-artifact
          github-token: ${{ github.token }}
          repository: JamieLittle16/Chess
          run-id: 34214504775
      - name: Download b050 search-leaf H64
        uses: actions/download-artifact@v4
        with:
          name: v15-python-searchleaf-h64-b050-94ef8e11b956050aaa057863010ce555d76733af
          path: model-artifact
          github-token: ${{ github.token }}
          repository: JamieLittle16/Chess
          run-id: 34290665543
      - name: Materialize presort control and b050 evaluator candidate
        shell: bash
        run: |
          set -euo pipefail
          ZIP=$(find v14-artifact -type f -name Little-Gambit-V14.zip -print -quit); test -n "$ZIP"
          MODEL=$(find model-artifact -type f -path '*/model/v14_student_h64.npz' -print -quit)
          if [ -z "$MODEL" ]; then MODEL=$(find model-artifact -type f -name v14_student_h64.npz -print -quit); fi
          test -n "$MODEL"
          rm -rf /tmp/base /tmp/cand results; mkdir -p /tmp/base /tmp/cand results
          unzip -q "$ZIP" -d /tmp/base; cp -a /tmp/base/. /tmp/cand/
          for D in /tmp/base /tmp/cand; do python scripts/patch_v15_python_presort_once.py "$D/experiments/numba_search.py"; done
          cp "$MODEL" /tmp/cand/experiments/v14_student_h64.npz
          python -m py_compile tools/v14_agent_runner.py /tmp/base/agent.py /tmp/cand/agent.py /tmp/base/experiments/*.py /tmp/cand/experiments/*.py
          python - <<'PY'
          import numpy as np
          a=np.load('/tmp/base/experiments/v14_student_h64.npz');b=np.load('/tmp/cand/experiments/v14_student_h64.npz')
          print({k:{'equal':bool(np.array_equal(a[k],b[k])),'max_abs_delta':int(np.max(np.abs(a[k].astype(np.int64)-b[k].astype(np.int64))))} for k in a.files})
          PY
      - name: Build fresh 96-root confirmation suite
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --location --retry 3 -o /tmp/uho.zip https://raw.githubusercontent.com/official-stockfish/books/65815ccdbc7727cd4f6aee252ba8f67fb740e92f/UHO_Lichess_4852_v1.epd.zip
          echo '4e298f11e8acfa106babe02968f2e61582145e7874c59284690b20b9650e0e07  /tmp/uho.zip' | sha256sum --check --strict
          rm -rf /tmp/m6; mkdir /tmp/m6
          python scripts/build_m6_opening_suite.py --archive /tmp/uho.zip --output-dir /tmp/m6
          python - <<'PY'
          from pathlib import Path
          old={x.strip() for x in Path('match/openings/m3-uho-lichess-100-v1.epd').read_text().splitlines() if x.strip() and not x.startswith('#')}
          large=[x.strip() for x in Path('/tmp/m6/m6-uho-lichess-5000-v1.epd').read_text().splitlines() if x.strip()]
          fresh=[x for x in large if x not in old]
          roots=fresh[2700:2796]
          assert len(roots)==96 and len(set(roots))==96
          Path('/tmp/searchleaf-b050-confirm.epd').write_text('\n'.join(roots)+'\n')
          print({'fresh_total':len(fresh),'slice':[2700,2796]})
          PY
      - name: 192-game competition-clock confirmation
        run: python -u scripts/v15_competition_gate.py --candidate /tmp/cand --control /tmp/base --openings /tmp/searchleaf-b050-confirm.epd --output results/timed-192.json --label searchleaf-b050-presort-confirm --base-ms 2500 --inc-ms 50 --max-plies 180 | tee results/timed-192.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: v15-python-searchleaf-b050-confirm-${{ github.sha }}
          path: results/
          retention-days: 30

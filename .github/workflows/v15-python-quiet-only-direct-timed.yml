name: V15 Python quiet-only direct qsearch timed qualification

on:
  push:
    branches: [analysis/v15-python-quiet-only-direct-timed-20260908]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  timed:
    runs-on: ubuntu-24.04
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install exact competition runtime
        run: python -m pip install --disable-pip-version-check chess==1.11.2 numpy==2.5.2 numba==0.67.0
      - name: Download exact packaged V14
        uses: actions/download-artifact@v4
        with:
          name: Little-Gambit-V14-PACKAGE-ae1dc230b9208f5ec46c9a661c3f0e526d6f0e56
          path: v14-artifact
          github-token: ${{ github.token }}
          repository: JamieLittle16/Chess
          run-id: 34214504775
      - name: Materialize exact V14 control and quiet-only candidate
        shell: bash
        run: |
          set -euo pipefail
          ZIP=$(find v14-artifact -type f -name 'Little-Gambit-V14.zip' -print -quit); test -n "$ZIP"
          rm -rf /tmp/base /tmp/cand; mkdir -p /tmp/base /tmp/cand results
          unzip -q "$ZIP" -d /tmp/base; cp -a /tmp/base/. /tmp/cand/
          python scripts/patch_v15_python_quiet_only_direct_qcheck.py /tmp/cand/experiments/numba_search.py
          python -m py_compile tools/v14_agent_runner.py /tmp/base/agent.py /tmp/base/experiments/*.py /tmp/cand/agent.py /tmp/cand/experiments/*.py
          cmp /tmp/base/agent.py /tmp/cand/agent.py
          cmp /tmp/base/experiments/numba_core.py /tmp/cand/experiments/numba_core.py
          cmp /tmp/base/experiments/v13_residual.i16 /tmp/cand/experiments/v13_residual.i16
          cmp /tmp/base/experiments/v14_student_h64.npz /tmp/cand/experiments/v14_student_h64.npz
      - name: Build fresh pinned 32-root timed book
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --location --retry 3 -o /tmp/uho.zip https://raw.githubusercontent.com/official-stockfish/books/65815ccdbc7727cd4f6aee252ba8f67fb740e92f/UHO_Lichess_4852_v1.epd.zip
          echo '4e298f11e8acfa106babe02968f2e61582145e7874c59284690b20b9650e0e07  /tmp/uho.zip' | sha256sum --check --strict
          curl --fail --location --retry 3 -o /tmp/build_m6.py https://raw.githubusercontent.com/JamieLittle16/Chess/91c8f05f6cebb78605f86c4b483d87d906533bcc/scripts/build_m6_opening_suite.py
          mkdir -p /tmp/m6; python /tmp/build_m6.py --archive /tmp/uho.zip --output-dir /tmp/m6
          echo '5445819229270140036023a507c41edffc9307ac2cca1ca2efd20e1ddb8d670e  /tmp/m6/m6-uho-lichess-5000-v1.epd' | sha256sum --check --strict
          python - <<'PY'
          from pathlib import Path
          old={x.strip() for x in Path('match/openings/m3-uho-lichess-100-v1.epd').read_text().splitlines() if x.strip() and not x.startswith('#')}
          large=[x.strip() for x in Path('/tmp/m6/m6-uho-lichess-5000-v1.epd').read_text().splitlines() if x.strip()]
          fresh=[x for x in large if x not in old];chosen=fresh[320:352]
          assert len(chosen)==32 and len(set(chosen))==32
          Path('/tmp/quiet-timed-32.epd').write_text('\n'.join(chosen)+'\n')
          PY
          sha256sum /tmp/quiet-timed-32.epd | tee results/timed-openings.sha256
      - name: Competition API timed A/B — 64 colour-swapped games
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json,math,queue,subprocess,sys,threading,time
          from collections import Counter,deque
          from pathlib import Path
          import chess
          BASE_MS=2500; INC_MS=50; MAX_PLIES=180; GRACE_MS=1200
          openings=[x.strip() for x in Path('/tmp/quiet-timed-32.epd').read_text().splitlines() if x.strip()]
          class Agent:
              def __init__(self,d):
                  self.p=subprocess.Popen([sys.executable,'tools/v14_agent_runner.py',d],stdin=subprocess.PIPE,stdout=subprocess.PIPE,stderr=subprocess.PIPE,text=True,bufsize=1)
                  self.q=queue.Queue(); self.err=deque(maxlen=100)
                  threading.Thread(target=self._out,daemon=True).start(); threading.Thread(target=self._err,daemon=True).start()
                  assert json.loads(self._line(95)).get('ready') is True
              def _out(self):
                  for x in self.p.stdout:self.q.put(x.rstrip('\n'))
                  self.q.put(None)
              def _err(self):
                  for x in self.p.stderr:self.err.append(x.rstrip('\n'))
              def _line(self,t):
                  try:x=self.q.get(timeout=t)
                  except queue.Empty as e:raise RuntimeError('timeout') from e
                  if x is None:raise RuntimeError('closed '+'\n'.join(self.err))
                  return x
              def reset(self):
                  self.p.stdin.write(json.dumps({'reset':True})+'\n');self.p.stdin.flush();assert json.loads(self._line(5)).get('reset') is True
              def move(self,b,ms):
                  self.p.stdin.write(json.dumps({'fen':b.fen(),'time_left_ms':max(0,int(ms))})+'\n');self.p.stdin.flush()
                  z=json.loads(self._line((max(0,ms)+GRACE_MS)/1000));m=chess.Move.from_uci(z['move']);assert m in b.legal_moves;return m
              def stop(self):
                  if self.p.poll() is None:self.p.kill()
                  self.p.wait(timeout=5)
          base=Agent('/tmp/base'); cand=Agent('/tmp/cand'); rows=[]; pairs=[]
          def play(fen,cw):
              b=chess.Board(fen);cl={chess.WHITE:float(BASE_MS),chess.BLACK:float(BASE_MS)};base.reset();cand.reset();tim=[]
              for _ in range(MAX_PLIES):
                  o=b.outcome(claim_draw=True)
                  if o is not None:return (0.5 if o.winner is None else float(o.winner==cw)),b.result(claim_draw=True),tim
                  side=b.turn;use=side==cw;e=cand if use else base;t=time.monotonic()
                  try:m=e.move(b,cl[side])
                  except Exception as ex:return (0.0 if use else 1.0),f'engine-failure:{type(ex).__name__}',tim
                  dt=(time.monotonic()-t)*1000;cl[side]-=dt
                  if cl[side]<0:return (0.0 if use else 1.0),'flag',tim
                  tim.append((use,dt));b.push(m);cl[side]+=INC_MS
              return 0.5,'max-ply-draw',tim
          try:
              for i,fen in enumerate(openings):
                  ps=0.0
                  for cw in (True,False):
                      s,r,t=play(fen,cw);ps+=s;rows.append({'opening':i,'candidate_white':cw,'score':s,'result':r,'candidate_ms':[x for c,x in t if c],'control_ms':[x for c,x in t if not c]});print(i,cw,s,r,flush=True)
                  pairs.append(ps)
          finally:base.stop();cand.stop()
          score=sum(r['score'] for r in rows);n=len(rows);f=score/n;elo=400*math.log10(f/(1-f)) if 0<f<1 else None
          ct=[x for r in rows for x in r['candidate_ms']];bt=[x for r in rows for x in r['control_ms']]
          out={'candidate':'V15 quiet-only direct qcheck','control':'exact packaged V14','games':n,'score':score,'score_fraction':f,'naive_elo_from_score':elo,'time_control_ms':[BASE_MS,INC_MS],'pair_histogram':dict(Counter(str(x) for x in pairs)),'terminations':dict(Counter(r['result'] for r in rows)),'candidate_mean_move_ms':sum(ct)/max(1,len(ct)),'control_mean_move_ms':sum(bt)/max(1,len(bt)),'details':rows}
          Path('results/timed.json').write_text(json.dumps(out,indent=2)+'\n');print('FINAL',json.dumps({k:v for k,v in out.items() if k!='details'}))
          PY
      - name: Build candidate submission ZIP
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [ -d /tmp/cand ]; then
            (cd /tmp/cand && zip -q -9 -r "$GITHUB_WORKSPACE/results/Little-Gambit-V15-python-quiet-direct.zip" .)
            sha256sum results/Little-Gambit-V15-python-quiet-direct.zip > results/package.sha256
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: v15-python-quiet-only-direct-timed-${{ github.sha }}
          path: results/
          retention-days: 30

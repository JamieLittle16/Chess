[package]
name = "chess-search"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[features]
default = []
search-trace = ["chess-eval/search-trace"]
# Research-only Search v2 move-ordering experiment. Default builds remain the accepted search.
context-history = []

[dependencies]
chess-core = { path = "../chess-core" }
chess-eval = { path = "../chess-eval" }

[lints]
workspace = true

# V16 Python next lanes

1. Qualify search-leaf H16 student. Promote only if both equal-node and competition-clock chess survive.
2. Independently qualify an exact Rust-style quiet history + one-ply continuation history port on the clean Python candidate.
3. If both are positive, test their interaction rather than assuming additivity.
4. Re-run direct Python-vs-Rust calibration only after a materially stronger combined Python candidate exists.

Tiny NPS-only tweaks are deprioritized because the measured -364 Elo calibration is dominated by chess quality rather than wall-clock overhead.
